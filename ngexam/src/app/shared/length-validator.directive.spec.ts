import { LengthValidatorDirective } from './length-validator.directive';

describe('LengthValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LengthValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
